# SQLite 파일 경로
DATABASE_URL="file:./dev.db"

# OpenRouter — 서버 라우트(/api/ai)에서만 읽는다. 클라이언트로 나가지 않는다.
OPENROUTER_API_KEY=""
OPENROUTER_MODEL="anthropic/claude-sonnet-4.5"
OPENROUTER_SITE_URL="http://localhost:3000"
