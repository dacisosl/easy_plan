# 의존성
node_modules/

# Next.js
.next/
out/
next-env.d.ts
*.tsbuildinfo

# 환경 변수 / 비밀 정보
.env
.env.*
!.env.example

# 데이터베이스
*.db
*.db-journal
prisma/dev.db*

# OS
.DS_Store
Thumbs.db
desktop.ini

# 편집기 / IDE
.vscode/
.idea/
*.swp

# 로그 / 임시 파일
*.log
tmp/
temp/

# 참고용 실물 계획서 — 교사 실명·학교명이 들어 있다.
# 저장소가 공개라 절대 커밋하지 않는다. 여기 넣고 스크립트로만 읽는다.
samples/

# 빌드 때 xlsx에서 굽는 정적 과목 데이터 (scripts/build-subjects.ts)
/public/data/
# 정적 배포 산출물
/out/
/public/form_2026.hwpx
/.firebase/

# 참고자료 실물 파일 — 교사 실명·학교명이 들어 있고 이 저장소는 공개다.
# 배포는 로컬 빌드에서 나가므로 앱에는 정상적으로 실린다. (README는 남긴다)
/public/refs/*
!/public/refs/README.md
