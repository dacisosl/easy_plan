# public/

정적 파일 자리. 여기 둔 파일은 `/파일이름` 으로 바로 열린다.

## school-logo.png

상단 바 왼쪽에 뜨는 **학교 마크**다. 이 이름 그대로 넣으면 자동으로 잡힌다.
없으면 청남색 '평' 사각형이 대신 뜬다 — 넣지 않아도 화면은 멀쩡하다.

- 권장: 정사각형 PNG, 배경 투명, 256×256 이상
- 화면에는 32×32로 줄여 그린다 (`object-contain`이라 비율은 안 깨진다)
